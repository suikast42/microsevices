info:
component: Enterprise configuration platform configuration serivce

jmx:
default_domain: eu.amova.cloud.platform

server:
  port: 9000

spring:
  cloud:
    config:
      server:
        git:
          #uri: https://github.com/suikast42/TestConfig.git
          #uri: ${user.home}\fake\configuration
          uri: ${user.home}\configuration
          clone-on-start: true

  application:
    name: configurationserver

debug: false
management:
  security:
    enabled: false




