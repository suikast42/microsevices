info:
component: Enterprise configuration platform configuration serivce

jmx:
default_domain: eu.amova.cloud.platform


server:
  port: 9000


spring:
  application:
    name: configurationserver

  profiles:
     active: native
  cloud:
    config:
      server:
        native:
          fail-on-error: true
        #  search-locations: file:///${user.home}/cloudenterprise/platform/configurations
       # search-locations:  file:///${user.home}/cloudenterprise/platform/configurations/{application}/{profile}
          search-locations:   file:///${user.home}/cloudenterprise/platform/configurations/{application}/{profile}


        #git:
          #uri: http://192.168.99.100/platform/platformconfig.git
          #uri: https://github.com/suikast42/TestConfig.git
          #uri: ${user.home}\fake\configuration
          #uri: ${user.home}\configuration
          #uri: file:///${user.home}/configuration
         # clone-on-start: true



debug: false
management:
  security:
    enabled: false




